import React from "react";
import API from '../utils/API';

const Location: React.FC = (): JSX.Element => { 

    return (
        <div>
            <input placeholder="What location would you like to see data for? Send a message to the dev."/>
        </div>
    )
}

export default Location;